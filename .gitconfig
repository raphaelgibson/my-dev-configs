[core]
	editor = code
[alias]
	c = !git add --all && git commit -m
	s = !git status -s
	l = !git log --pretty=format:'%C(blue)%h %C(red)%d %C(white)%s - %C(cyan)%cn, %C(green)%cr'
	fe = !git fetch origin
	dev = !git fetch origin && git rebase origin/develop
	pu = !git push origin HEAD
